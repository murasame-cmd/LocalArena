plugins { id 'com.android.application' }

android {
    namespace 'com.localarena'
    compileSdk 36
    defaultConfig {
        applicationId 'com.localarena'
        minSdk 26
        targetSdk 36
        versionCode 1
        versionName '0.1.0'
    }
    buildTypes { release { minifyEnabled false } }
}
